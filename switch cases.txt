#include<stdio.h>

int main()
{

    if (n <= 1) {
        return 0;
    }

    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            return 0;
        }
    }

    return 1;

int n =0;
printf("enter the number");
scanf("%d",&n);

return 0;
b=1;

if (n < 0)
        printf("Enter a number greater than 0");
    else {
        for (a = 1; a <= n; ++a) {
            b *= a;
        }
        printf("Factorial of %d = %d", n, b);
}
